{
  "name": "@boringhand/next-t2-template",
  "private": true,
  "scripts": {
    "web:dev": "pnpm dev --filter web",
    "web:build": "pnpm build --filter web",
    "desktop:dev": "pnpm dev --filter desktop",
    "desktop:build": "pnpm build --filter desktop",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "devDependencies": {
    "prettier": "^3.4.2",
    "turbo": "^2.4.0",
    "typescript": "5.7.3"
  },
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=18"
  }
}
